const Overlay = () => {
  return (
    <section className="page">
      <h1>Overlay</h1>
      <p>
        Design the broadcast overlay components used to display champions, timers, and player info for spectators.
      </p>
      <p>Ensure this view stays lightweight so it can render in streaming software without extra configuration.</p>
    </section>
  )
}

export default Overlay
